# For You 💖

Simple single-page static Valentine proposal.

Preview
- Open `index.html` in a browser, or run a local server:

```bash
python -m http.server 8000
# then open http://localhost:8000
```

What’s included
- `index.html` — single-file demo with CSS and JS.

Next steps
- Add a remote and push the `main` branch.
- Add a license if you want to publish.

Enjoy! 💕
